version: "3.9"
services:
  api:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        - COCKROACHDB_CLUSTER_ID=${COCKROACHDB_CLUSTER_ID}
    env_file:
      - .env
    ports:
      - "${PORT}:${PORT}"
